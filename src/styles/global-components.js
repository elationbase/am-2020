import styled from 'styled-components';
import theme from './theme';

export const OffsetSection = styled.header`
  background-color: ${theme.palette.dark};
  transform: skewY(-2deg) translateY(3rem);
`;
